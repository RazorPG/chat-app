# Nuxt Chat App (starter)

Simple starter for a chat application using Nuxt 3 + Tailwind + MongoDB.

Environment variables (example):

```
MONGODB_URI=
JWT_SECRET=
CLOUDINARY_URL=
PUSHER_APP_ID=
PUSHER_KEY=
PUSHER_SECRET=
PUSHER_CLUSTER=
```

Development:

```bash
npm install
npm run dev
```
